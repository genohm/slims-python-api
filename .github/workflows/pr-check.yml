name: Run tests on PR

on:
  push:

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-python@v5
      with:
      python-version: '3.13' 
    - name: Install tox
      run: pip install tox
    - name: Run tests
      run: tox
